// here you can add a connection to redis
// this is just an example of something else that could go into the server folder
// you can also add implementations for stuff like aws, some other service/microservices, etc.

/*
https://redis.io/docs/connect/clients/nodejs/
import { createClient } from 'redis';

const client = createClient();

client.on('error', err => console.log('Redis Client Error', err));

await client.connect();

export default client;
*/
